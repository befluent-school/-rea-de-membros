# Configuração de Produção - Be Fluent School
# Copie este arquivo para .env e configure as variáveis

# Ambiente
NODE_ENV=production

# JWT Secret - GERE UMA CHAVE FORTE!
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-must-be-at-least-32-characters-long

# Database URL - Configure sua conexão PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/banco
# Exemplo: postgresql://user:password@localhost:5432/befluentschool
DATABASE_URL=postgresql://username:password@host:port/database

# Porta do servidor (opcional)
PORT=5000

# Configurações de CORS (opcional)
CORS_ORIGIN=https://yourdomain.com

# Configurações de Rate Limiting (opcional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Configurações de Sessão (opcional)
SESSION_SECRET=your-session-secret-key

# Configurações de Upload (opcional)
MAX_FILE_SIZE=10485760
UPLOAD_PATH=uploads/

# Configurações de Email (opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Configurações de Log (opcional)
LOG_LEVEL=info
LOG_FILE=logs/app.log
